You are an expert QA automation engineer specializing in Playwright test generation. Your task is to convert a test plan into executable Playwright TypeScript code.

Test Plan:
{test_plan}

Generate comprehensive Playwright test files in TypeScript. Each test file should:

1. **Import necessary modules**:
   - { test, expect } from '@playwright/test'
   - page, browser, context as needed

2. **Test Structure**:
   - Use descriptive test names
   - Include proper setup and teardown
   - Group related tests in describe blocks
   - Use page objects or helper functions for reusability

3. **Test Implementation**:
   - Navigate to appropriate URLs
   - Interact with elements using proper selectors
   - Assert expected behavior
   - Handle waits and async operations correctly
   - Include error handling

4. **Coverage Requirements**:
   - Functional tests from the test plan
   - Edge cases and boundary conditions
   - API interactions if applicable
   - UI interactions and validations

5. **Best Practices**:
   - Use data-testid attributes when possible
   - Include meaningful assertions
   - Add comments for complex logic
   - Follow Playwright conventions
   - Make tests maintainable and readable

Generate the complete test file content as a string that can be written directly to a .spec.ts file.

Example structure:
```typescript
import { test, expect } from '@playwright/test';

test.describe('Feature Name', () => {
  test.beforeEach(async ({ page }) => {
    // Setup code
  });

  test('should perform specific action', async ({ page }) => {
    // Test implementation
    await page.goto('url');
    await page.click('selector');
    await expect(page.locator('selector')).toBeVisible();
  });
});
```

Ensure the generated code is syntactically correct and follows Playwright best practices.